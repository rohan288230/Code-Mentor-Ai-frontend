# Copy this file to `.env` in the same folder (`server/.env`) and fill in values.
# Never commit real passwords or API keys.

MONGO_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/code_mentor_ai?retryWrites=true&w=majority
PORT=5000
SESSION_SECRET=use-a-long-random-string-in-production
CLIENT_ORIGINS=https://code-mentor-ai-murex.vercel.app/
OPENAI_API_KEY=
